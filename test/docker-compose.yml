version: "3.8"
services:
  db:
    image: mysql:8.0
    restart: always
    ports:
      - "4306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-mypassword}
      MYSQL_DATABASE: ${DB_NAME:-mydb}
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
