name: Deploy to NPM

on:
  push:
    branches:
      - main

permissions:
  contents: read
  id-token: write # ⭐ [핵심] OIDC 인증을 위한 필수 권한

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"
          # ⭐ setup-node가 이 url을 보고 OIDC 토큰을 자동으로 생성해서 연결해줍니다.

      # lock 파일 문제 방지를 위해 install로 변경 (ci로 해도 무방하지만 안전하게)
      - run: npm install

      # 👇 [수정됨] env 설정을 완전히 삭제했습니다.
      # setup-node가 이미 인증 준비를 마쳤으므로 명령어만 실행하면 됩니다.
      - run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_KEY }} # Optional: If you want to use a secret token as a fallback
