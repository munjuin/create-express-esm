name: Deploy to NPM

on:
  push:
    branches:
      - main # ⚠️ 본인의 기본 브랜치 이름과 같아야 합니다 (main 또는 master)

# 👇 이 부분이 제일 중요합니다 (NPM과 신뢰 연결)
permissions:
  contents: read
  id-token: write

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "20"
          registry-url: "https://registry.npmjs.org"

      - run: npm ci

      # 👇 --provenance 옵션이 있어야 Trusted Publishing이 작동합니다.
      - run: npm publish --provenance --access public
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
